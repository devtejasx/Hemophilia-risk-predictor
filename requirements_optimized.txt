# ============ OPTIMIZED REQUIREMENTS FOR PRODUCTION DEPLOYMENT ============

# Core FastAPI framework with async support
fastapi==0.104.1
uvicorn[standard]==0.24.0
uvloop==0.19.0  # Faster event loop
httptools==0.6.1  # For uvloop support

# Production ASGI server
gunicorn==21.2.0

# ML/Data Science (optimized versions)
scikit-learn==1.3.2
xgboost==2.0.3
pandas<3  # Streamlit compatibility
numpy==1.24.3
shap==0.43.0  # Explainability

# Data visualization
plotly==5.17.0
matplotlib==3.8.1
seaborn==0.13.0

# Database
sqlite3-python==1.0.0
sqlalchemy==2.0.23  # ORM support

# Streamlit frontend
streamlit==1.28.1

# PDF generation
reportlab==4.0.9

# Data modeling/validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Security & Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# OpenAI API
openai==1.3.7

# Caching & Performance
cachetools==5.3.2

# Concurrency & Background tasks
celery==5.3.4  # For production background tasks (optional)
redis==5.0.1  # For Celery broker (optional)

# Development & Monitoring
python-dotenv==1.0.0
python-multipart==0.0.6

# Logging
python-json-logger==2.0.7

# Production deployment utilities
gunicorn-debugger==1.0.1  # Optional debugging
psycopg2-binary==2.9.9  # PostgreSQL support (optional)
pymongo==4.6.1  # MongoDB support (optional)

# Performance monitoring
prometheus-client==0.19.0  # Metrics

# Health checks
requests==2.31.0

# Testing (for CI/CD)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Code quality
black==23.12.0
flake8==6.1.0
mypy==1.7.1

# ============ INSTALLATION INSTRUCTIONS ============

# For development:
# pip install -r requirements_optimized.txt

# For production minimal install (faster):
# pip install -r requirements_optimized.txt --no-deps

# Or install just essentials:
# pip install fastapi uvicorn gunicorn scikit-learn xgboost pandas numpy shap streamlit plotly reportlab pydantic openai python-dotenv requests

# ============ OPTIONAL PERFORMANCE PACKAGES ============

# Orjson (faster JSON serialization)
# orjson==3.9.11

# Async database drivers
# asyncpg==0.29.0  # PostgreSQL async
# motor==3.3.2  # MongoDB async

# Caching backends
# aiocache==0.12.2
# aioredis==2.0.1

# Compression
# brotli==1.1.0  # Better compression than gzip
# zstandard==0.21.0

# ============ DOCKER-OPTIMIZED REQUIREMENTS TYPE ============

# For container deployment with minimal image size:
# Use multi-stage build and install only prod packages

# Core production packages only:
# fastapi
# uvicorn[standard]
# gunicorn
# scikit-learn
# xgboost
# pandas
# numpy
# shap
# plotly
# streamlit
# reportlab
# pydantic
# openai
# python-dotenv
# requests

# ============ DEPLOYMENT VERIFICATION ============

# After installation, verify with:
# python -c "import fastapi, uvicorn, sklearn, xgboost, shap, streamlit, plotly; print('✅ All packages installed')"

# Check versions:
# pip list | grep -E 'fastapi|uvicorn|gunicorn|scikit-learn|xgboost|pandas|numpy|shap|streamlit|plotly'

# Verify async support:
# python -c "import uvloop; print('✅ uvloop available for faster asyncio')"

# Check compression:
# python -c "import gzip, brotli; print('✅ Compression libraries available')"
