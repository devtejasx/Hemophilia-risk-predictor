# Development: training, evaluation and tests. Installs runtime deps too.
-r requirements.txt

# --- Additional model families compared during model selection ---
# catboost has moved to requirements.txt: it won selection for the fused model,
# so it is needed to unpickle that artifact at inference too.
lightgbm==4.7.0
imbalanced-learn==0.14.2

# --- Plots produced by the training/evaluation scripts ---
matplotlib==3.10.7

# --- Tests ---
pytest==9.0.0
httpx==0.28.1
