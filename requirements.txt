# Runtime dependencies: FastAPI backend + CHAMP inference + explanations.
# Training-only and test-only dependencies live in requirements-dev.txt.
#
# scikit-learn is pinned exactly: the serialised artifacts under ml/artifacts/
# were fitted with 1.8.0 and a minor-version drift changes unpickling behaviour.

# --- API ---
fastapi==0.141.1
uvicorn[standard]==0.42.0
pydantic==2.13.5
python-dotenv==1.2.3
python-multipart==0.0.20
email-validator==2.3.0

# --- Auth ---
PyJWT==2.13.0
bcrypt==5.0.0

# --- Data / inference ---
pandas==2.3.3
numpy==2.3.3
scikit-learn==1.8.0
xgboost==3.2.0
joblib==1.5.3

# --- Explainability ---
shap==0.51.0
lime==0.2.0.1
